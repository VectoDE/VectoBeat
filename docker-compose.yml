version: "3.9"

services:
  vectobeat:
    build: .
    container_name: vectobeat
    restart: unless-stopped
    env_file:
      - .env
    # The bot does not expose HTTP services; no ports are published by default.
    # Override command or add volumes here if you need additional integration.
