import { buildPageMetadata } from "@/lib/seo"

export const metadata = buildPageMetadata({
  title: "Stats | Live VectoBeat Telemetry",
  description: "Live VectoBeat telemetry: guilds, listeners, uptime, and performance metrics from production.",
  path: "/stats",
  keywords: ["vectobeat stats", "discord bot telemetry", "lavalink metrics", "vectobeat uptime"],
})
