import { buildPageMetadata } from "@/lib/seo"

export const metadata = buildPageMetadata({
  title: "Account | VectoBeat",
  description: "Manage your VectoBeat account, subscriptions, and Discord bot settings.",
  path: "/account",
  noindex: true,
})
