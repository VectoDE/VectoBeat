{
  "extends": ["next/core-web-vitals"],
  "rules": {
    "no-unused-vars": "warn",
    "no-console": "warn",
    "prefer-const": "error"
  },
  "ignorePatterns": ["node_modules", ".next", "dist", "build"]
}